# Amazon-Golf-Cutting-Problem
The C++ code for the Amazon "Golf Cutting" problem.
Idea is rather simple. Search the matrix once, record the heights
Then sort, and call BFS between every two adjacent heights
